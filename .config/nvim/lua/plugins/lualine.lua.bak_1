return {
    'nvim-lualine/lualine.nvim',
    dependencies = { 'nvim-tree/nvim-web-devicons' },
    config = function()
        require('lualine').setup({
            options = {
                theme = 'onedark_vivid', -- Match lualine with the onedarkpro theme
                section_separators = '|',
                component_separators = '',
            },
        })
    end,
}
